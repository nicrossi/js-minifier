function compute() {}
function fallback() {}
function log() {}
function cleanup() {}

try {
  let attempts=0, MAX=3;
  let value = compute() || fallback();   // if compute() short-circuits
  if (!value && attempts < MAX) {
    throw "retry";
  }
} catch (e) {
  log(e);
} finally {
  cleanup();
}
