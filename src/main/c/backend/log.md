# Backend - Status Report

## Goal
The AST (Abstract Syntax Tree) is generated by the parser, becomes the primary input for this backend phase. The goal is to transform the AST into a more compact representation, optimizing it for size while preserving the semantics of the original JavaScript code.

## 1 · Current coverage
| **Feature area**         | **What we need**                                                            | **Status & notes**                                                                               |
|--------------------------|-----------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|
| **Entry-point**          | `generateProgram()` traverses AST and delegates to emit helpers             | ✔ Done (using `_output` with 0 indentation)                                                      |
| **Overall coverage**     | Cover all features of [frontend log](../frontend/syntactic-analysis/log.md) | 🟡 Simply outputing the code without spaces or indentation (complex minfication pending)         |
| **Expressions**          | Emitters for every `ExpressionType`                                         | ✔ `_genExpression` plus helpers (*identifier, array, call, new, unary, binary, logical &&/‖*)    |
| **Statements**           | Generator for all `StatementType`                                           | ✔ Basic set (`if`, loops, `return`, `break`, `continue`, `throw`, `try-catch-finally`, `switch`) |
| **Boolean**              | Replace boolean literal `true/false` with, `!0/!1`                          | ✔ Done                                                                                           |
| **Numeric folding**      | Replace `1+2` with `3`, etc.                                                | ✔ Done                                                                                           |
| **String concatenation** | Replace `"foo" + "bar"` with `"foobar"`, etc.                               | ✔ Done                                                                                           |
| **Name shortening**      | Replace variable and function names with a minified version                 | 🟡 Replacing ALL names sequentially (future iteration should keep already short names unchanged) |
| **Valid JS code check**  | Check that the input is well formed and valid JS code                       | ✔ Basic (check variable duplicates in scope, missing declaration)                                |
| **Testing**              | Basic suite with When/Expect test                                           | ✔ Done. With basic test cases                                                                    |

_Last updated: 2025-06-22_
